public interface Calc2D {

    static double circleArea(Circle circle) {
        return (circle.getX() * circle.getX()) * circle.pi;
    }

    static double rectangleArea(Rectangle rectangle) {
        return (rectangle.bok * rectangle.getX());
    }
}
